<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>people</title>
    <link rel="stylesheet" href="/css/master.css" type="text/css">
  </head>
  <body>
  <div class="main-container">
    <% include header.ejs %>
    <p>
      Voici la liste des étudiants de simplon
    </p>
    <div class="people-container">
      <% for(var i=0; i<people.length; i++) { %>
        <a href= <%= 'users/' %><%= people[i].id %><%= '/project' %>>
          <%= people[i].firstName %>
        </a>
      <% } %>
    </div>
  </div>
  </body>
</html>
