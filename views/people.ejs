<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>people</title>
    <link rel="stylesheet" href="/css/people.css" type="text/css">
  </head>
  <body>
    <p>Voici la liste des étudiants de simplon
    </p>
      <ul>
        <% for(var i=0; i<people.length; i++) { %>
          <li><%= people[i].id %>
            <%= people[i].firstName %>
          </li>
          <% } %>
        </ul>

  </body>
</html>
