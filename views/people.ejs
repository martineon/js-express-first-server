<p>Voici la liste des étudiants de simplon

  <ul>
  <% for(var i=0; i<people.length; i++) { %>
      <li><%= people[i].id %>
          <%= people[i].firstName %>
      </li>
  <% } %>
  </ul>

</p>
